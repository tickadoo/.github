version: 2
updates:
  # This repository has no package manifests; keep the pinned action SHAs
  # (and their version comments) current.
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "weekly"
    open-pull-requests-limit: 5
    commit-message:
      prefix: "chore(actions)"
